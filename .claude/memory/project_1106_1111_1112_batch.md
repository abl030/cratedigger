---
name: project-1106-1111-1112-batch
description: 2026-08-13 six-PR batch shipped+deployed+closed; five correction rounds each minted a false claim; combined re-gate ran on epi after doc1 OOM
metadata:
  type: project
---

2026-08-13: #1106/#1111/#1112 closed via six PRs (#1114 rules, #1123 triage-cancel, #1124 unfindable K=240+metrics, #1125 submit-retry consolidation, #1128 copy-marker audit, #1129 suite admission), deployed nixosconfig `b1d75b4e`, cycle-verified, both new web surfaces confirmed live (triage status route; dashboard `unfindable` block — empty until the first nightly K=240 run populates it, worth a later glance).

- **Five consecutive correction rounds each minted >=1 new false absolute**, every one caught only by the next independent read — the "no round catches its own false claims" rule (shipped in #1114, itself corrected by review: only 1 of its 4 cited incidents matched its original scope) is now validated in-batch. Verification reviews of correction rounds are non-negotiable.
- **#1111 item 2's premise was false**: the copy-marker audit passes GREEN on the real pre-fix #1086 tree (same-file variant needs site-level attribution = prohibited scanner). Recorded in the issue close comment; the audit guards the fully-inert-marker class only.
- The unfindable timeout math in issue #1112 (~29s/probe) was the degraded 409-storm run; healthy nominal is 52-59s/probe from journal. Never relay an issue-body number into a brief unverified ([[feedback-orchestrator-briefs-become-defects]]).
- moduleVm failed on epi (TCG): endpoint-drift warning count assertion at test.py:3860 -> issue #1130 (cross-linked #1127). Deploy proceeded on rendered-config + suite + cycle evidence instead.
- Main moved constantly under the batch (parallel sessions merged #1113-#1126); PR 2 needed a woven conflict resolution + merged-tree re-gate; PR 1/PR 4 needed in-flight main merges. Budget for moving main on multi-session days.
