---
name: project-711-decision-state
description: Issue
metadata:
  node_type: memory
  type: project
  originSessionId: 9cad5dde-7262-46ba-ba9d-5c1cc038e558
---

2026-07-16: the full #711 decision surface was settled in-thread (7 comments posted this session after a verified code audit; every claim in the earlier Codex discussion checked out against code). Read the thread before any #711 implementation — it is the authoritative record.

Key settled shape: verified-lossless proof is the ONLY terminal acquisition stop and the boundary is absolute (both directions); no acceptance floor and no terminal accept for unverified lossy (gate_min_rank retired outright); one narrowing rule (transparent+genuine → lossless-only, everywhere); analysis failure aborts loudly on BOTH sides and never mints verification (spectral None/error no longer verify); acquisition facts (proof + lossless-source V0 anchor) carry unconditionally across evidence rebuilds — no audio-identity check (evil-maid is outside the state model) — and the request-scalar seeding dies after one deploy backfill; `have_analysis_error` is mechanically an ordinary failed attempt (backoff + global cooldown apply; good-citizen) but semantically non-quality (no denylist/narrowing/verdict); every retained lossy import denylists its source; Vorbis AND WMA first-class (WMA bands = MP3-CBR values), generic relabel-as-MP3 fallback deleted, UNKNOWN = orderable bottom for decodable-unranked codecs; V0 override 230/200 stays hardcoded deliberately (guards the terminal stop); provisional imports set lossless-only override at import time.

Regression contract: 5 named change families (verified inversion; terminal lossy accept abolished — canonical flipping pin `test_mp3_v0_240`; grade-blind CBR narrowing loosened; absence-verifies inverted — `test_flac_no_spectral_is_verified`; lossless re-import over proof inverted — `test_genuine_flac_reimports_verified`). Everything else in the corpus is protected.

Live facts that shaped decisions: wanted=1,404 (NOT the stale ~4,700); wanted∧verified proof = 1 row (reconciliation is a glance, not a backfill); imported∧verified = 7,061/7,068 so abolishing terminal lossy accept barely moves production; zero WMA/MPC/APE ever measured (WMA promoted on operator experience anyway).

2026-07-16 (later, ce-brainstorm session): the last two open items settled and the whole surface consolidated into `docs/plans/2026-07-16-001-refactor-quality-model-untangle-plan.md` (PR #731, merged; settlement comment posted on the thread). Vocabulary: two-axis — subject (`installed`|`source`) on spectral + V0 (one persisted marker each, no column split; proof is always source), provenance (`measured`|`carried`) on spectral, V0, and proof. V0's three lineage values collapse into the subject axis (native/on-disk research distinction dropped, confirmed); `unknown_v0_source` and `v0_proof_provenance` die; `verified_lossless` leaves `AudioQualityMeasurement` (proof object sole owner, row bool derived + CHECK-tied). Migration: `lineage_version` bump + existing rebuild-on-touch machinery converts rows lazily; field-level DDL deferred to ce-plan. NOTHING on the #711 decision surface remains open — implementation starts from the plan artifact.

2026-07-16 (later still): plan enriched to implementation-ready via ce-plan (PR #733, merged): 8 KTDs, 11 units (U1-U11, 6 suggested PRs), complete named corpus flip-sets enumerated per family, Verification Contract + DoD. Key research findings baked in: preview skips spectral for ALL non-MP3 today (absence-verifies fires on the happy path — KTD1 relocates the scan to preview and retires the harness re-scan); post-import action mapping duplicated in 4 places (KTD2 consolidates, with an independent expectation-table oracle so parity isn't self-confirming); migration 055 needs value maps for `unknown_v0_source` + migration-024's `new_measurement_fallback` and version-gated CHECKs. Implementation next: /orchestrate-issue or ce-work from the plan.

2026-07-17 (post-review settlement, decisions 17-20): a 13-lane multi-agent review + 12-validator pass of the implementation branch (`refactor/711-quality-model-untangle`; report committed at `docs/reviews/2026-07-17-711-quality-model-review.md`, decisions recorded in the plan's Key Decisions and an issue #711 comment) found two implementation deviations and settled four new decisions. D17: narrowing is SUBJECT-BLIND — transparent+genuine narrows to lossless-only at import time (AE6's letter; the shipped installed-subject deferral was reverted). D18: the post-import evidence-unavailable path reopens to wanted with NO denylist — local bookkeeping failure never blames a peer. D19 (the operator's emphatic one): force-import overrides the beets distance and NOTHING else — no separate code path/state, never terminal; operator imports run the identical post-import gate and canonical retained-import mapping (only the stage-2 proof-lock bypass survives, per D1/U7). D20: D5 reaffirmed — have_analysis_error keeps feeding the global cooldown streak (good-citizen: don't hammer a peer while the local install is busted); the two cooldown writers now share one streak evaluator. Also: migration 055 amended pre-ship (converted rows seed spectral source/carried — blanket 'installed' would have dropped protective source grades on first rebuild); post-import policy cluster extracted to `lib/dispatch/post_import.py`; each decision is its own loud commit (37ec02fa, f1487528, ea1a6dc9, 756edf68, c787b603; branch head 6c9e9a9e). Branch NOT yet merged/deployed; deploy gates in the review doc (pg_dump before 055, wrapper gateMinRank check, moduleVm, timer-hold → U11 one-shot → reconciliation glance).

2026-07-17 (later): a Fable unauthorized-change audit (76 deltas traced code→authority, committed as docs/reviews/2026-07-17-711-unauthorized-change-audit.json) found NO further force-import-class regressions. It surfaced the KTD4/U7 "force-import bypasses the proof lock" clause as planning-time widening → operator settled D21 (commit f3efc23b): the verified-lossless proof lock is ABSOLUTE for every import mode; force-import bypasses only the beets distance; Replace/re-request is the only way back in (D1's letter restored). Wrong-Matches cleanup unchanged (disk-loaded short-circuit stays; backfilled proof deletes through the reducer's locked decision per D6). Open question deferred to its own PR: renaming operator-facing "force import" labels (recommendation: UI/CLI copy only, keep internal/audit vocabulary). Also: scripts/fuzz_burst.sh added (core-matched parallel Hypothesis burst — never run the burst serially).

2026-07-18 (proof-mint incident, PR #738 — merged, deployed, live-repaired): the untangle (be206853) minted the verified-lossless proof from `args.filetype`, an attribute the harness argparse never defines — AttributeError fired EXACTLY when will_be_verified_lossless was True, the preview degraded the crash to evidence_ready (87a99dc1), and proof-less candidate rows persisted → no proof lock → the pipeline re-replaced its own verified installs every cycle (Passenger req 8877 2× in 3h; Glasgow School 2776; 12×5 8878 + Power Windows 8879 poisoned-candidates-only). Fix: pure `mint_verified_lossless_proof` in lib/quality/decisions.py (source = was_converted_from → detected format → sentinel, old 850dc83a semantics); decision="crash" is fail-closed at three layers (worker→measurement_failed, `evidence_from_import_result`→"crashed_result" dominating gate, classify→uncertain); authorized import carries the candidate row's proof into its emitted ImportResult. The load-bearing test lesson: only a REAL-main()+sys.argv pin catches the argparse-drift class — synthetic Namespaces grow whatever attribute code asks for. Live repair one-shot: proofs stamped on 5 candidate + 2 installed rows, 8877/2776 flipped to imported+verified (055 CHECK: TRUE needs provenance+source+classifier, FALSE needs all four NULL). Deploy note: `pin_nixosconfig.sh` follows MAIN — a branch rev cannot be pinned; merge first (found when the pin died with "status=" empty). Fallout issues: #739 (Replace on never-imported backfill row leaves old install; new pressing imported into the SAME folder — Passenger dir now interleaves beets albums 3607 MP3 + 19571 Opus, operator decision pending), #737 comment (argparse-drift structural guard candidate).

2026-07-18 (later — provisional surfacing, PR #740, merged+deployed): operator flagged the provisional engineering had decayed. Audit: decision core intact (new provisional imports → lossless-only + denylist; rejection ladder never widens), but (a) NO persistent UI marked installs provisional/verified — badges.js had no identity axis at all (#575 PR1 removed the last provisional strings from web/js), and (b) the legacy cohort (54/70 wanted provisional requests) carried pre-#711 tiered overrides still downloading MP3s that can never verify (Guapo's salvared MP3). Shipped: `get_pipeline_overlay` derives verified_lossless/provisional_lossless from linked current evidence (evidence-only; fake parity + real-PG pins), overlay stamps `pipeline_verified_lossless`/`pipeline_provisional`, badges.js chips (green `verified` / pale-green `provisional`, screenshot-loop verified on Guapo+Passenger), no-widening generated property (no resolution path ADDS a lossy tier; redirect-to-lossless legal). One-shot: 54 requests → 'lossless' override (consumed per-cycle at enqueue — NO plan invalidation needed, lib/enqueue.py effective_search_tiers), + 1,553 evidence rows stamped `was_converted_from` from download_log-provable lossless ancestry (whole early-era library lacked the FLAC→ lineage, not just the cohort). Ratchet lesson: any line shift in lib/pipeline_db/*.py needs the replaced-write audit re-key (file:line:hash), and sequential str.replace re-keying can collide (1310→1325 then 1325→1340 double-hit).

2026-07-18 (latest — Replace status-gate fix, PR #741, merged+deployed): operator ruled the Passenger replace failure "a regression — replace REPLACES". RCA precision: NOT a code edit — the `old_status == "imported"` cleanup gate shipped WITH the U4 service (28fb5fc3, 2026-05-18) when "wanted ⇒ nothing on disk" was true, and the 2026-06-04 full-library backfill silently falsified it (~4,700 wanted rows tracking pre-existing installs). Fix: cleanup keys on `old_release_id` resolving in beets only, never lifecycle status (`remove_and_reset_release` no-ops when absent; clear_pipeline_state=False audit-freeze unchanged; downloading Replace can't touch in-flight transfers — removal only sees beets state). Pair: Passenger pin + hypothesis property over all four source statuses; two integration slices pinning the old behavior flipped. #739 retitled to the remaining defect: same-path import doesn't disambiguate when a different-MBID album occupies the rendered folder. Lesson worth keeping: when a cohort-creating backfill lands, audit every status-keyed conditional whose invariant the backfill changes ("wanted means nothing on disk" died 17 days after being encoded).

2026-07-18 (final — aunique collision fix, PR #742, merged+deployed, #739 CLOSED): the disambiguation half of the Passenger incident. RCA (scratch-reproduced against the real pinned beets + production config): beets `%aunique` picks the FIRST disambiguator field whose values are all-distinct across the same-key set, then renders each album's OWN value — an EMPTY value renders NO bracket → plain stem → import lands inside the sibling's sticky folder. Passenger: old label='ATO Records' vs new label='' → label "won" → empty bracket. Any empty-capable field in the disam list is poison; the July pairs survived by luck of field values. Fix: single computed inline field `path_disambig = albumdisambig or releasegroupdisambig or catalognum or label or str(year)` (never empty; ties → beets' non-empty album-id bracket); short_mbid retired (Discogs albums have empty mb_albumid = same poison). NOT a code regression: the old cratedigger post-import move belt died by cascading dead-code decay (call disconnected → 01ede8d0 deleted helper → #352 swept move_album) but was NOT the cause — import-time aunique worked for months after. Guard: TestAuniqueCollisionContract (real-beets subprocess, extracts the SHIPPED template+album_fields from nix/module.nix, 512-world exhaustive sweep, old template = permanent known-bad; invariant: no same-key sibling ever renders its plain stem). Experiment technique worth reusing: scratch BEETSDIR + pinned beets env + real run_beets_harness.sh driven by a hand-rolled JSON protocol driver reproduces production import behavior exactly; the Jul-9 beets DB backup recovered the deleted album's fields.

Related: [[project-723-blank-source-path-evidence]].
