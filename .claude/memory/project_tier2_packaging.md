---
name: project-tier2-packaging
description: Tier-2 packaging COMPLETE 2026-07-04 — U12 cutover verified live on doc2, tagged v2026.07.04; HM beets gone
metadata: 
  node_type: memory
  type: project
  originSessionId: 9b4f3237-d43d-4e33-9122-ddbeaf1f253d
---

Plan at `docs/plans/2026-07-03-001-feat-tier2-packaging-plan.md`. **COMPLETE 2026-07-04: U1–U11 merged (PRs #482–#492), U12 cutover executed live on doc2 with the operator, first tag `v2026.07.04` cut.** Cutover facts: both equivalence gates passed (rendered config BYTE-IDENTICAL to the retired HM config; `beet move --pretend` output identical under both stacks, 415/415 lines); nixosconfig commits bffb8834 + 9d83abf2 (the group-permissions src patch needed a context refresh — U5's docstring reword broke its hunk; Forgejo blocks force-push to master, so fixes go as follow-up commits); HM beets removed from doc2 (doc1's personal HM beets kept); discogs token at /var/lib/cratedigger/secrets/discogs-token (root 0400, extracted from old secrets.yaml — sops migration pending); verified live: pinned beets 2.12.0 + 15 plugins via cratedigger-beet (run it with sudo — the token secrets.yaml is root-0400, non-root gets the discogs OAuth prompt), 2 real imports evidence-authorized end-to-end with Plex/Jellyfin scans firing, sane live distances (high_distance reject at 0.2372), MB+Discogs browse through the new api_base wiring. README rewritten tier-2 quickstart-first + examples/ (cratedigger, discogs-mirror, musicbrainz-mirror samples) in PR #493.

What landed: flake.lock at nixpkgs 2026-06-29 (**beets 2.12.0**, matching prod) with plain `nix-shell` pinned via a flake.lock-reading shim; module builds from own lock (`packageSet` + escape hatch); `nix/beets.nix` owned beets (mirror knobs `beets.discogsMirrorUrl`/`lrclibUrl`; tokenless default renders a placeholder discogs user_token because the plugin runs interactive OAuth at LOAD otherwise); module renders beets config.yaml into `${stateDir}/beets` (BEETSDIR) — verified **byte-identical** to doc2's live HM config with operator values substituted (`pkgs.formats.yaml` = HM's generator); every HM seam deleted (BEETSDIR + `[Beets] config_dir/beet_binary/python`; grep-guard `tests/test_hm_coupling_guard.py` bans `/home/abl030`, `/etc/profiles/per-user`, `.beet-wrapped` in code); `musicbrainz.apiBase`/`discogs.apiBase` one-value threading incl. headless consumers (`web/api_bases.py` — pipeline-cli and youtube worker call it at startup); `pipelineDb.createLocally` (role+db = cfg.user, peer auth) + friendly assertions; `packages.default` CLI bundle (`nix run .#pipeline-cli`); `scripts/pre-push` runs `nix flake check` (installed on doc1); moduleVm = stranger-boot gate; `docs/mirrors.md` runbook.

**U12 cutover extras beyond the plan text** (from reviews): nixosconfig wrapper must set `musicbrainz.apiBase = http://192.168.1.35:5200`, `discogs.apiBase = https://discogs.ablz.au`, `beets.discogsMirrorUrl`, `beets.lrclibUrl = http://192.168.1.35:3300/api`, `beets.discogsTokenFile` — without the apiBase pair, web+CLI fall back to public MB / Discogs-off. Remove HM beets module in the SAME commit. Verify Replace-picker distances live: U5 made cratedigger-web's in-process beets read the rendered config, so match.preferred penalties are newly active in distance scores. Clean the wrapper's stale sqlx comment + unused PGPASSWORD injection. Cut the first `vYYYY.MM.DD` tag after live verification.

Gotcha worth keeping: beets interactively prompts "Create it (Y/n)?" when the library's PARENT dir is missing — blocks any non-interactive shell (hung the VM 30 min before diagnosis).
