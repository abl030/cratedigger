---
name: project-575-ui-consolidation
description: Issue
metadata: 
  node_type: memory
  type: project
  originSessionId: 2f201277-ea14-4a12-96d9-f567f6debb65
---

Issue #575 ("UI Changes - Lots") is a vision doc turned into a 6-phase plan of record (posted as issue comment 2026-07-10). Verdict: **fix, not rewrite** — debt is concentrated in frontend renderers (5 bespoke release-row renderers, 6 expand-on-click toggles, triplicated track-meta block, 3 detail panels); backend seams for unification already exist (`LibraryAlbumRow`, `annotate_in_library`, `merge_discographies`, `analyse_artist_releases`).

**Framing correction:** no top-level Library/Queue tabs exist — Library/Analysis/Compare are Browse sub-views; "Queue" = Recents→import-jobs timeline + Pipeline→requests list.

Phases: 1) demolition ✅ · 2) Recents standardisation · 3) shared render primitives (`renderReleaseRow`/`renderTrackRow`/generic `toggleExpand`) · 4) unified artist page (absorb Library/Analysis/Compare; explicit "missing" list is pure presentation over `annotate_in_library`) · 5) absorb queues + reconcile the two badge vocabularies · separate issue) beets wrappers (fetchart/lyrics) — MUST ship as importer-queue job types (beets-mutating boundary) with CLI⇄API pairs.

**PR1 SHIPPED** (#594, merged+deployed+tagged v2026.07.10-4, −3,190 lines): Decisions tab end-to-end + 4 dead routes (`/api/pipeline/recent`, `/api/manual-import/scan`, `/api/beets/search`, `/api/beets/recent`) with full orphan cascade (`lib/manual_import.py`, `enrich_with_pipeline`, `apply_pipeline_bitrate_override`, `BeetsDB.search_albums/get_recent`, `PipelineDB.get_recent`, fakes/self-tests/projection-registry entry).

**Lesson — "zero frontend callers" ≠ dead:** the sweep shrank ~11→4 after checking (a) CLI⇄API twins (disk-coverage, status, triage list, manual-import, import-preview, search-plan dry-run/saturation all pair with pipeline-cli commands) and (b) payload-embedded URLs (`/api/wrong-matches/audio` is live via the explorer's `stream_url`). Always run both checks before deleting a route.

**#612 honest-metrics SHIPPED** (v2026.07.11-3, 2026-07-11): operator asked "what's 216k and what's 255k" on the same card — the persisted basis said "avg 216k" (decision layer fabricated avg=min in synthesized comparison measurements, lib/quality/pipeline.py + decisions.py), the Bitrate row never said min, and candidate research V0 probes were hidden as "audit-only". Fixed: unmeasured stats stay None (basis labels min honestly; value-neutral under avg/min metrics), "Min bitrate" row + `min 216k` strip labels, V0 rendered for every kind with "(from lossy)"/"(on-disk re-encode)" qualifiers (matching Wrong Matches). Pin+property pair (assert_basis_metrics_truthful, mutant-qualified); 5-row psql one-shot relabeled the since-#608 mislabeled bases. Also #613: run_tests.sh truncates $OUT up front (early JS-gate failure left a stale false-green output file). See [[measured-evidence-only]].

**#610 detail-story fix SHIPPED** (v2026.07.11-2, 2026-07-11): operator hit a Rejected Recents card (req 8781, mbid_missing) whose expanded detail told a quality-success story — #608's basis "Compared" row + blue "verified lossless bypass" up top, actual reason a dim line below the grid, Download History three mobile-screens under the 14-track library list. Fix: verdict now leads each history entry (red via `.p-hist-verdict-reject` on rejected/failed/timeout/user_offline/curator_ban), Download History moved above tracks in toggleDetail, beets_detail surfaced as forensics Detail row. Principle for PR5: any view showing an outcome shows its reason at the same prominence. library.js panel deliberately stays tracks-first (noted on #603). Also: user feedback — never draft PRs ([[no-draft-prs]]).

**PR2b SHIPPED** (#596, v2026.07.10-6): operator hit "256kbps (was 256kbps)" on an Upgraded card — rank-driven upgrade (AAC 256 genuine replacing unverified MP3 256) with the deciding metric (on-disk codec) invisible. `existing_format` (from import_result.existing_measurement.format JSONB) now threads ClassifiedEntry → log route → DownloadHistoryViewRow → HAVE side ("HAVE MP3 256k") + Bitrate was-suffix ("was MP3 256kbps"). Note: V0-probe row hidden for native_lossy_research_v0 kinds is BY DESIGN (only lossless_source_v0 probes render).

**PR4e rg-row request badges SHIPPED** (#606, v2026.07.10-12): backend rg-row overlay in web/routes/browse.py — `_artist_pipeline_maps` (one list_requests_by_artist query; by mb_release_group_id + by mb_release_id for masterless Discogs rows; downloading>wanted>manual>imported; replaced excluded) applied in get_artist/get_discogs_artist/_overlay_compare. Zero frontend changes (renderStatusBadges reads pipeline_status when present; 🔍 inspector appears via pipeline_id). Known gap: Discogs MASTER rows can't match (requests key on release ids). Also fixed: .lib-track-heading teal needed a two-class selector to beat .release-detail .lib-track specificity.

**PR4d masterless overlay SHIPPED** (#605, v2026.07.10-11): masterless Discogs releases (loadReleaseGroup's synthesized single-pressing row) dropped the pipeline/library overlay the API correctly returned → green Add on an already-wanted release (Deloris 'Feather Figure/Elastic Bones', 8838). Fix = exported pure `synthesizeMasterlessRow` forwarding the overlay (new tests/test_js_discography.mjs). Also: Discogs index/heading tracks (track_number 0) render as unnumbered `.lib-track-heading` rows. Lesson: any SYNTHESIZED row must forward the payload's overlay fields — the projection-drift class again, frontend edition. **PR4c toggle hint SHIPPED** (#604, v2026.07.10-10): Source label + tooltips + flipping primary/complement hint; applySourceUI consolidates both source-flip call sites (authored by the playwright agent via an operator chat mixup; reviewed + shipped by orchestrator).

**PR4b dedupe fix SHIPPED** (#602, v2026.07.10-9): live poking found the Deloris 1998 split double-rendering (Bootleg-only badged + Library-only editions). Orphan title-dedupe set only covered the inLibrary bucket; now spans every in_library-annotated RG whatever bucket (bootlegs/appearances). Lesson: a dedupe guard scoped to one bucket of a partition silently misses the others — key the guard on the ANNOTATION, not the bucket.

**PR4 SHIPPED** (#601, merged+deployed+tagged v2026.07.10-8, 2026-07-10; live round exercised the Only-on-Discogs complement end-to-end — 79 releases on The Lucksmiths, forced-source pressing loads working, compare 200, zero console exceptions): unified artist page replaces the 4 Browse sub-tabs. Sections: In library (+Library-only editions orphan fallback, invariant I6) / In flight (downloading+manual ONLY — wanted is ambient post-backfill, stays a badge) / Missing (new surface off annotate_in_library) / Appearances / Bootleg-only; background decorations (token-guarded, cached, never re-render): compare complement → appended "Only on Discogs/MB" section (silent 503 degrade), disambiguate → unique/covered chips + expansion dots/recordings. Library-tab deep detail = lazy panel in renderReleaseDetail via /api/beets/album. ZERO backend changes. Demolished: switchSubView+4 loaders, compare machinery, renderArtistDiscography, renderDisambiguateInto/toggleDisambRGTracks, renderLibraryResults(Into), state.browseSubView, orphaned CSS. Review lessons: cache-hit paths must re-fire null-cached decorations (navigate-away-early trap); owned-album visibility needed the orphan fallback; token bumps on every teardown path. 3 screenshot rounds (Lucksmiths/Mach‐Hommy/417.3). Next: PR5 queues + badge vocabulary.

**PR3 SHIPPED** (#600, merged+deployed+tagged v2026.07.10-7, 2026-07-10): `web/js/render_primitives.js` = `renderReleaseRow` (adopted by discography renderRelease, analysis renderDisambRG, labels rows), `renderBeetsTrackRow`/`renderExpectedTrackRow` (+fixes the 1:60 duration bug), generic `toggleExpand` (replaces 4 toggle bodies; analysis panel style.display→.release-detail class), `renderDetailRow`/`renderExternalLinkRow`, `toggleSection` (7 inline copies; bound on window in main.js). Library/pipeline/rg/compare rows deliberately NOT converted — they adopt the primitive in PR4/PR5 when those pages restructure. 68-assertion mjs test RED-first; 2 screenshot rounds (loop caught the analysis star/title flex split — .lib-track is flex justify-between, marker+title must share one span); Opus review clean. Also removed stray `pr2c-recents-final.png` that #595 committed to the repo root. Pre-existing wart for PR4: disabled-button overlap on 3-button pressing rows @1000px.

**PR2 SHIPPED** (#595, merged+deployed+tagged v2026.07.10-5, 2026-07-10): IN/HAVE strips (numeric-gated), fixed detail vocabulary, server-badge headers, `overridden` distance, forensics toggle, centering ROOT CAUSE (containers kept `class="loading"` → text-align:center forever), badge humanize, dev-server per-thread-BeetsDB fix. **Verification method that worked: 3 screenshot rounds against `web_dev_server.py --data live-db` (PG tunnel from doc1 + CDP chromium + playwright agent) BEFORE pushing — caught the centering root-cause and the missing `downloaded_label` forwarding.** `slskd_bitrate` NULL in all rows — capture REJECTED by operator (#599 won't-do: advertised metadata is untrustworthy, measurement-only model); the dead plumbing is slated for DELETION in #598. Next: PR3 shared render primitives.

**Original PR2 spec (for reference):** the mess is prose-from-many-code-paths + per-outcome ad-hoc grids + two disagreeing outcome vocabularies (list badge `Failed` vs detail `timeout`; `web/classify.py` vs inline recents.js/pipeline.js badges). Fix = one fixed evidence-first attempt schema rendered identically in list+detail: IN (format, measured bitrate, spectral grade, slskd-advertised) / HAVE / DIST (force imports: "overridden · measured X", never 0.000 — `history.js` renders `beets_distance` unconditionally) / WHY (classifier-derived one-liner); forensics (stages, plan items, raw slskd errors) behind a toggle. Payload already carries everything incl. never-rendered `slskd_bitrate` and V0 min/median (`DownloadHistoryViewRow` in `web/download_history_view.py`). Extends #564's evidence-derived-messages doctrine. Visual audit screenshots from 2026-07-10 in `~/cratedigger/.playwright-mcp/`.
