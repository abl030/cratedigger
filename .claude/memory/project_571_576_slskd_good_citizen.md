---
name: project-571-576-slskd-good-citizen
description: "Issues #571 + #576 BOTH CLOSED 2026-07-10 (v2026.07.10-2): good-citizen ledger doctrine shipped across 7 PRs — deploy-kill fix, search ledger+sweep, transfer ledger, 3 seam flips, docs flip; all live-verified"
metadata: 
  node_type: memory
  type: project
  originSessionId: bdf6c249-a96e-4b43-aefe-678211da0c3b
---

**COMPLETE 2026-07-10 ~05:20 AWST, all live-verified on doc2.** PRs: #582 deploy-kill (v2026.07.09-5), #583 search ledger+sweep (#576 closed, 1,675-search purge, v2026.07.09-6), #585 transfer ledger (v2026.07.09-7), #586 convergence flip + #587 reaper flip (v2026.07.10), #588 per-id completed purge (transfer_id T1.5/T2 capture), #589 close-out (stranded review fixes cherry-picked + docs flip; #571 closed; v2026.07.10-2). Live: COMPLETED-PURGE removes own stamped records, foreign counted-never-touched; SEARCH-LEDGER sweep closed-loop. **Process lessons:** (1) NEVER `git push | tail -N` inside a && chain — the pipe masks a push failure; bit twice (nixosconfig push raced the rolling bot mid-deploy; PR #588 merged WITHOUT its review commit — recovered by cherry-pick into #589). (2) Sonnet session limits killed two implementers mid-PR; orchestrator hand-finishing from their worktrees worked both times. (3) Deferred one-shot: pre-ledger download-dir debris (mtime < 2026-07-09 ~23:30 AWST, unledgered, outside failed_imports/) is permanently unreapable under the new doctrine — operator clears deliberately if du warrants.

**Progress 2026-07-10 (early):** #571 flips: PR #586 (convergence → ledger-owned strays only, foreign never cancelled; get_owned_transfer_keys) and PR #587 (reaper → positive ownership only: stamped local_paths + get_owned_attempt_folders canonical derivation; unowned files survive forever) both merged + deployed + live-verified, tagged v2026.07.10. PR #588 (per-id completed purge + transfer_id T1.5/T2-COALESCE capture, bulk remove_completed_downloads deleted) in opus review — sonnet died at session limit mid-self-review AGAIN (2nd time; resets hit at 9:30pm and 2:30am Perth), orchestrator hand-finished the post-#587 merge + shipped. Decision: skipped the pre-ledger-debris deploy one-shot (cohort = days-old small failed-attempt debris; leaving files is safe under new doctrine, hand-rolled deletes at 1am are not — recipe: on-disk files with mtime < migration-045 deploy (2026-07-09 ~23:30 AWST), not in ledger local_paths/attempt folders, outside failed_imports/ + active attempts). Remaining: merge+deploy #588, PR 6 docs flip (README dedicated-instance rewrite, CLAUDE.md "#550 cratedigger-owned" phrase, "5-min timer"→OnUnitInactiveSec=1s, "pairs" wording), close #571.

**Progress 2026-07-09 (late):** PR #582 (deploy-kill: requires→wants + `assert_schema_current` gate) merged+deployed+tagged v2026.07.09-5, live-proven (next switch left the mid-flight cycle running). PR #583 (#576: migration 044 write-ahead search ledger, mint-per-attempt at all POST sites, `converge_slskd_searches` Phase 0c, fake-delete fidelity, I1/I2/I3 pin+property pairs) merged+deployed+tagged v2026.07.09-6; one-shot purge deleted 1,675 residents (1,679→4); 26 ledger rows write-ahead on first new-code cycle. #576 CLOSED. Remaining: #571 flips — PR2 transfer ledger (brief at jobs/bdf6c249/tmp/pr2-brief.md, implementer running), PR3 convergence flip, PR4 reaper flip, PR5 per-id completed purge, PR6 docs flip. Orchestration: sonnet implement (worktree) → opus review loop → I verify+merge+deploy; sonnet hit session limit once mid-PR1 (orchestrator finished by hand — audit ALLOWLIST precedent for asymmetric-seam reads used for record_search_id/mark_search_ids_deleted).

2026-07-09 planning session (original findings below). #571 = every cleanup must only touch what cratedigger instantiated (reverses #550's "cratedigger owns the slskd instance" doctrine, README + reaper + convergence). #576 = slskd searches leak.

**#576 RCA (live-verified on doc2):** 1,677 searches resident; 1,673 TimedOut/Errored, oldest 2026-04-03; slskd `retention.search` is UNSET (keep forever — nixosconfig slskd.nix has no retention block). Leak is ONGOING (~20-40/day) post-#466 (PR #503 moved delete into finally, 2026-07-04). Dominant path: **process SIGTERM/kill mid-cycle leaks all in-flight searches** — search ids are memory-only, never persisted anywhere (no DB column). Verified: 17:41:26 SIGTERM (the #570 deploy) leaked exactly the 4 in-flight searches. Other leak paths: L1 submit retry mints a fresh uuid4 per attempt (half-created search orphaned), L2 SearchSubmitError before try/finally, L4 pool.submit failure post-create.

**Seam inventory (#571):** Indiscriminate seams = A `converge_slskd_orphans` (lib/slskd_transfers.py:424 — cancels any live transfer not in currently-`downloading` rows' active_download_state), B `reap_disk_orphans` (:708 — deletes any 7-day-old file not in protected set), D `remove_completed_downloads` (slskd_client.py:375, bulk DELETE all completed transfer records every cycle at cratedigger.py:1487 — why the reaper had to exist). Already ownership-scoped by construction: C cancel_and_delete, F search stop/delete, G failed_imports quarantine, H materialize pruning. **Structural gap: nothing persists "cratedigger enqueued (username,filename,transfer_id)" beyond the downloading window.**

**Plan direction:** one doctrine — persisted positive-ownership ledger (searches: write-ahead the client-minted uuid before POST, reuse one uuid across retries; transfers/files: ledger row at enqueue, event-stamped local_path). Then: converge_slskd_searches sweep (delete completed slskd searches whose id ∈ ledger — heals ALL leak paths incl. kills), flip A/B to ledger-positive ownership (unknown = untouchable), replace D's bulk purge with per-id remove of owned transfers. Docs flip (README dedicated-instance section). Tests to invert: tests/test_repair.py unowned⇒orphan pins, tests/test_disk_reaper_generated.py ORPHAN-zone worlds; FakeSlskdSearches.delete only records, doesn't remove — fix for fidelity. One-shot purge of the 1,662 residents at deploy (agent one-shot, per [[feedback-single-operator-no-backfill-scripts]]). Optional backstop: slskd retention.search in nixosconfig.

**Bonus bug (distinct, found while RCA'ing the kill):** every deploy SIGTERMs a mid-flight cycle despite `restartIfChanged=false` — `nix/module.nix:1418-1419` has `requires = ["cratedigger-db-migrate.service"]` and the migrate unit restarts on every switch (store path in ExecStart changes every code bump), so systemd Requires= stop-propagation kills cratedigger.service. Cycles run back-to-back (`OnUnitInactiveSec=1s`, ~4.5min cycles — CLAUDE.md's "5-min timer" is stale), so a switch lands mid-cycle ~always; with ~250s of each cycle in the search phase, most deploys leak in-flight searches. Same requires+restartIfChanged=false combo on cratedigger-unfindable (module.nix:1475-1476). Fix direction: wants+after instead of requires, plus an explicit fail-loud schema-version gate at app startup (migrate gate invariant preserved); the ~2s "crash-loop" seen 17:41-17:45 was just the 1s timer re-firing a preStart that failed on #570-transition permissions (known lesson, fixed same day).

Minor doc drift found: CLAUDE.md "batch searches in pairs" — actual structure is serial submit + search_max_inflight(4) collect pipeline; also "5-min timer" vs OnUnitInactiveSec=1s continuous cycling.
