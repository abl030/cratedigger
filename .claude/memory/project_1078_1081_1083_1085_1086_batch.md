---
name: batch-1078-1081-1083-1085-1086
description: "2026-08-12: six PRs for #1081/#1078/#1085/#1086/#1083 shipped and live-verified; 11 review rounds, every round found real defects; residuals in #1094-#1100, #1106, #1111"
metadata:
  type: project
---

Shipped 2026-08-12, main `c476f924`, signed nixosconfig `a40a6433`, cycle
`bb7d55a3` verified live on doc2.

PRs: #1097 (#1081 selector + the #1086 item 4 flake bound), #1101 (#1078),
#1102 (#1085), #1107 (#1086 item 1), #1109 (#1083), #1110 (#1086 items 2+3).

**The dominant defect class, four PRs out of five: a check that cannot fail.**
Not missing tests — tests that exist, pass, and are structurally incapable of
failing on the thing they name. #1081's pin resolved against a module set that
made the honest mapping impossible, so it enforced the dishonest one. #1083's
property cancelled either before entry or after each delete returned, so a
mid-delete mutant survived all 8 tests; the fix then patrolled one instant and a
differently-placed mutant still passes. #1107's browser property accepted either
copy string, and its marker constant had never matched the paragraph it policed.
#1110 asserted a total bucket partition while two outcome shapes landed in zero
buckets. Every one was found by planting a mutant, never by reading.

**Correction rounds introduce false claims at roughly the rate they fix them** —
four instances, each caught by the *next* independent read, never by the round
that wrote it. Reviewing the claims a correction commit adds is now the
highest-yield part of a final round.

**Deploy gotcha worth repeating:** another agent's PR merged into main between
my gate and my PR merge, so GitHub's merge tree differed from the gated tree
(compare `git rev-parse <a>^{tree} <b>^{tree}`). Re-gate combined main — and
rebuild the module VM if `nix/module.nix` moved — before pinning.

Residuals filed rather than shoehorned: #1094, #1095, #1096, #1098, #1099,
#1100, #1106, and the reflection #1111 (shared-host tmpfs contention, an inert
copy marker, two rule amendments). #1077 parked by the operator.

Related: [[orchestrator-briefs-become-defects]], [[pin-and-fuzz-pair]].
