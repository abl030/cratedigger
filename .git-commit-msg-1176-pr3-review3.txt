fix: local-import review round 3 — F6 fail-closed, false docstring, containment guard (issue #1176 PR3)

Targeted review of d4fc9fae confirmed force is byte-identical (proven by
mutant both directions) and the F4 relocation is correct (probed live:
the moved copy satisfies open_configured_quarantine_directory and
unsafe_failed_import_path_reason). Four things left, all fixed here.

1. HIGH — F6's comment denied a case that exists, and the leak was live.
   _action_copy_front_gate's own except branch (raw_path resolved BEFORE
   _prepare_force_action_path runs) is exactly the "copy doesn't exist
   yet" case the prior round's comment claimed couldn't happen. When
   _prepare_force_action_path itself fails (operator folder moved/renamed
   between enqueue and preview claim — realistic with a backed-up preview
   queue — a symlink/special file inside it, EACCES, ENOSPC), the alias
   fell back to front_gate_source (the raw operator path), leaking it
   into preview_result.source_path, the MeasurementFailure blob, and
   download_log.staged_path. Fixed: front_gate_audit_source now fails
   closed for local_import when front_gate_action is None — resolves the
   deterministic force_action_copy_path this job would have retained had
   preparation succeeded, and falls back to the documented "not resolved"
   sentinel ("", the same one MeasurementFailure.source_path's own
   docstring defines) only if resolving even that fails. Corrected the
   docstring at both sites (the inline comment and _JobActionAuthority's
   own) to name the except branch instead of denying it. New pin
   (tests/test_import_queue.py) deletes the operator's source folder
   between enqueue and preview claim so BOTH the front-gate's first
   attempt and execute_preview_job's internal retry genuinely fail —
   confirmed RED via planted mutant (reverting to the denied-case alias
   reproduces the exact leak), GREEN on the fix.

2. MEDIUM — "reaches NEITHER" was false, and the removed:true receipt
   needed its precondition stated. A strict-validation reject IS still a
   terminal failure bundle, so scripts/importer.py::
   _cleanup_terminal_force_action DOES run for it via the importer's
   shared terminal path — it just finds nothing, because relocation into
   wrong_matches/ always runs FIRST, synchronously, inside
   _dispatch_import_from_db_locked, before the terminal bundle it returns
   is ever persisted. The safety here is ORDERING, not structural
   impossibility, and the docstring now says so explicitly: if a future
   change ever persisted the terminal bundle before relocating, that same
   cleanup call would delete the operator's own Wrong Matches folder,
   destroying the escape hatch this delta exists to create. Corrected the
   matching claims in docs/rejection-routing.md and
   docs/pipeline-db-schema.md.

3. LOW — the relocation call had no containment guard of its own. Safe
   today only because the strict-validation guard is its one caller and
   failed_path is always the job's own private action copy; every sibling
   mutator of this namespace asserts its own containment
   (remove_force_action_copy checks dirname(path) !=
   processing_albums_dir(...)). Extracted the check into its own function
   (_assert_local_import_relocation_containment) — both because a
   one-line precondition belongs in one place, not duplicated at a second
   call site later, and because extracting it makes the guard directly
   unit-testable without any PipelineDB/dispatch fixture at all. Pinned
   in tests/test_dispatch_from_db.py (accept-the-job's-own-copy +
   known-bad refuse-a-lane-path-directly, the latter proven via planted
   mutant).

4. LOW — nix/module.nix:813-819's own localImportReadOnlyPaths
   definition-site comment still said "the importer and preview-worker
   units" after the web unit was added to the bind set — the one comment
   directly above the binding, which is what the next reader sees first.
   Corrected; comment-only, no moduleVm re-run needed.

Typing ratchet: reused (and extended) the file's own established
Any-typed bridge, tests/helpers.py::finalize_claimed_dispatch, for both
new mandatory-slice tests instead of calling process_claimed_job
directly — the crash-path test needed the extension (passing a
BaseException instance instead of a DispatchOutcome now raises it
through the real process_claimed_job; no existing caller passes one, so
every existing call site is unchanged). That brought
tests/test_integration_slices.py's type_ignore count from 37 back to
35 — the exact original baseline, not merely below the flagged 37.
The one remaining new ignore this round would have come from item 3's
first draft (an integration-slice pin needing the same FakePipelineDB
bridge for dispatch_import_from_db); extracting the guard into its own
pure function and unit-testing it directly eliminated that need
entirely, so the tests baseline for test_integration_slices.py,
test_dispatch_from_db.py, and tests/helpers.py are all unchanged from
their pre-review state — the ratchet did not move at all.

Full canonical suite green (nix-shell run_tests.sh: js-syntax, js-unit,
pyright whole-tree + production-strict, ruff, vulture, complete python
scheduler, all PASSED).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
